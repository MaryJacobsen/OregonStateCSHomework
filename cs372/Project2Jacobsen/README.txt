ftserver.c and ftclient.py must be in different directories.

To compile ftserver, type make. To run it type ./ftserver [server port]

To run ftclient, type python ftclient.py [server host] [server port] [command] [filename (if command is -g)] [data port]
 
